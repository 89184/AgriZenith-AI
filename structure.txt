AgriBoot/
├── backend/                          # Core logic (intent, retrieval, LLM, etc.)
│   ├── __init__.py
│   ├── config.py                     # Environment variables, Pinecone/Groq config
│   ├── constants.py                  # Intent keywords
│   ├── data_loader.py                # Load & split documents (PDF/TXT)
│   ├── embeddings.py                 # Embedding models (LangChain + Sentence‑Transformers)
│   ├── entity_extractor.py           # Semantic entity extraction from queries
│   ├── generator.py                  # Groq LLM client
│   ├── intent_classifier.py          # Hybrid intent classifier (fuzzy + embedding + zero‑shot)
│   ├── retriever.py                  # Pinecone vector retrieval
│   ├── pipeline.py                   # Full RAG pipeline orchestrator
│   └── main.py                       # FastAPI server entry point
│
├── config/                           # Configuration files
│   ├── config.yaml                   # (optional YAML config)
│   └── logging.conf                  # (optional logging config)
│
├── data/                             # Document storage
│   ├── raw/                          # Place your PDF/TXT files here
│   └── processed/                    # (optional) preprocessed data
│
├── frontend/                         # All frontend clients
│   ├── app/                          # Desktop app (PyQt5)
│   │   ├── __init__.py
│   │   ├── agriboot_desktop.py       # Entry point
│   │   ├── windows/
│   │   │   ├── __init__.py
│   │   │   └── main_window.py        # Main chat window
│   │   ├── widgets/
│   │   │   ├── __init__.py
│   │   │   ├── chat_bubble.py        # Message bubble + metadata card
│   │   │   └── typing_indicator.py   # Typing animation
│   │   ├── threads/
│   │   │   ├── __init__.py
│   │   │   └── api_thread.py         # Async API request thread
│   │   ├── dialogs/
│   │   │   ├── __init__.py
│   │   │   └── history_dialog.py     # History viewer
│   │   └── utils/
│   │       ├── __init__.py
│   │       └── config.py             # API_URL from .env
│   │
│   ├── web/                          # Static web frontend (HTML/CSS/JS)
│   │   ├── __init__.py
│   │   ├── index.html
│   │   ├── style.css
│   │   └── app.js
│   │
│   └── (streamlit)                   # (Optional) Streamlit app – can be added later
│
├── scripts/                          # Utility scripts
│   └── build_index.py                # Build Pinecone index from documents
│
├── tests/                            # Unit tests (optional)
│   └── ...
│
├── validation/                       # Validation/test pipeline (as in your structure)
│   └── ...
│
├── .env                              # Environment variables (API keys, API_URL)
├── environment.yml                   # Conda environment definition
├── requirements.txt                  # Python dependencies
├── README.md                         # Project documentation
├── LICENSE                           # License file
└── structure.txt                     # This file (project tree)

----------------------------------------------------------------
Key files explanation:
- backend/main.py     → FastAPI server, exposes /ask and /health
- backend/pipeline.py → orchestrates intent, entities, retrieval, LLM
- frontend/app/       → PyQt5 desktop app (WhatsApp‑style chat)
- frontend/web/       → static HTML/JS website
- scripts/build_index.py → builds Pinecone index from raw documents
- .env                → set PINECONE_API_KEY, GROQ_API_KEY, API_URL
----------------------------------------------------------------